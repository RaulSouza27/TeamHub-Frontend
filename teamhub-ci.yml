###################################################
#               Stage Declarations               #
###################################################
stages:
  - deploy

###################################################
#           Stage: deploy                         #
###################################################
docker-deploy:
  stage: deploy
  only:
    - master
    - develop
  tags:
    - teamhub
  script:
    - docker compose up --build -d
  environment:
    name: production
